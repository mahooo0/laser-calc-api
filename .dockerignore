# Local dev artefacts
.venv/
.git/
.gitignore
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Tests are not needed inside the runtime image
tests/

# Environment / secrets — must come from runtime, not baked into the image
.env
.env.local
.env.*.local

# Misc
.DS_Store
*.md

# Runtime data — orders should be a volume, not part of the image
orders.csv

# Self
Dockerfile
docker-compose.yml
.dockerignore
